# Copy to .env.local and fill in (Razorpay Dashboard → Settings → API Keys).
# Server-only: never prefix these with NEXT_PUBLIC_. Use rzp_test_… keys until go-live.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
